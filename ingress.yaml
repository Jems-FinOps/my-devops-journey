apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: coffee-ingress
spec:
  rules:
  - host: coffee.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: coffee-app
            port:
              number: 80
